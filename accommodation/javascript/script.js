// JavaScript Document
function validate(){
	var float=parseInt(document.getElementById('float').value);
	var cash=parseInt(document.getElementById('cash').value);
	var total=float+cash;
	document.getElementById('total').value=total;
}
function dates() {
	var from=getElementById('total').value;
	alert(from);
}